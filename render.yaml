services:
  - type: worker
    name: kasbiy-talim-ishonch-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "bot.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
   
